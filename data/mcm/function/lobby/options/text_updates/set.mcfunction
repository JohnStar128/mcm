#> Copies the text in `text` to the text displays `text`
#> Needed to resolve the text component

#> Useful for simple number displays

data modify entity @s text set from entity @s Passengers[0].data.text